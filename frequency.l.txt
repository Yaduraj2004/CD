%{
#include <stdio.h>
#include <string.h>
int count = 0; 
%}

%option noyywrap

%%
"word_to_count" { count++; }
.|\n {}
%%

int main() {
    yylex();
    printf("Frequency of the word: %d\n", count);
    return 0;
}
